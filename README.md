# bigquery-terraform-dataengineering-project

## CLEAN ENVIRONMENT

```bash
    echo $GOOGLE_CREDENTIALS 
    unset GOOGLE_CREDENTIALS (unset PROXY)
```

```bash
terraform destroy
```

terraform plan would return an **error**
